hallo

Identifier · com.alexa.all_in_poster

To get started run:
  cd all_in_poster
  npm install
  npm run tauri android init

For Desktop development, run:
  npm run tauri dev

For Android development, run:
  npm run tauri android dev

  stack
    Base = tauri
    Front end lang = js\ts
    Manager = npm + cargo
    UI Template = React
    Ui Flavor = TypeScript